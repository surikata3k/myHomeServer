services:
  samba:
    image: dockurr/samba
    container_name: samba
    environment:
      NAME: "Data"
      USER: "public"
      PASS: "123456"
    ports:
      - 445:445
    volumes:
      - /server/files:/storage
    restart: always
